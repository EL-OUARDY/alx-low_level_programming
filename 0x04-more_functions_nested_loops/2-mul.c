#include "main.h"
/**
 * mul - basic multiplication
 * @a: integer is passed as an argument
 * @b: integer is passed as an argument
 * Description: receive two interger and do math multiplication
 * Return: int
 */
int mul(int a, int b)
{
	return (a * b);
}
