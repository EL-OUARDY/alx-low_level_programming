#include <stdio.h>
#include "main.h"
/**
 * main - type ny name ^^
 * @argc: the number of arguments
 * @argv: array of passed arguments
 * Description: just type the program name
 * Return: integer always 0
*/
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
