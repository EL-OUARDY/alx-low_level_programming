#include "main.h"
/**
 * _memset - fill memory with a specific character
 * @s: starting address
 * @b: the desired character
 * @n: number of bytes
 *
 * Return: array with new value
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
